import { FC } from 'react';

interface CompetitorListProps {}

const CompetitorList: FC<CompetitorListProps> = ({}) => {
  return (
    <div>CompetitorList</div>
  );
};

export default CompetitorList;