export enum PATHS {
  ROOT = '',
  HOME = 'home',
  AUTH = 'auth',
  LOGIN = 'login',
  DASHBOARD = 'dashboard',
  TEST = 'test',
}

export enum ROUTES {
  ROOT = '/',
  HOME = '/home',
  LOGIN = '/auth/login',
  DASHBOARD = '/dashboard',
  TEST = '/test',

}