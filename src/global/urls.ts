export enum PATHS {
  ROOT = '',
  HOME = 'home',
  AUTH = 'auth',
  LOGIN = 'login',
  DASHBOARD = 'dashboard',
}

export enum ROUTES {
  ROOT = '/',
  HOME = '/home',
  LOGIN = '/auth/login',
  DASHBOARD = '/dashboard',
}